version: "3.9"

services:
  nebula:
    build: .
    image: nebula:latest
    container_name: nebula_app
    environment:
      - DATABASE_URL=postgresql+asyncpg://nebula:nebula_secret@postgres:5432/nebula
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - WEAVIATE_URL=http://weaviate:8080
      - NVIDIA_API_KEY=${NVIDIA_API_KEY:-}
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      weaviate:
        condition: service_started
    command: python scripts/run_benchmark.py

  postgres:
    image: postgres:16-alpine
    container_name: nebula_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: nebula
      POSTGRES_USER: nebula
      POSTGRES_PASSWORD: nebula_secret
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nebula -d nebula"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  weaviate:
    image: semitechnologies/weaviate:1.24.1
    container_name: nebula_weaviate
    ports:
      - "8080:8080"
    environment:
      QUERY_DEFAULTS_LIMIT: 25
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: "true"
      PERSISTENCE_DATA_PATH: /var/lib/weaviate
      DEFAULT_VECTORIZER_MODULE: none
      ENABLE_MODULES: ""
    volumes:
      - weaviate_data:/var/lib/weaviate
    restart: unless-stopped

volumes:
  postgres_data:
  weaviate_data:
